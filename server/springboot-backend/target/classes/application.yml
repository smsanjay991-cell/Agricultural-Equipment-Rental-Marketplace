server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agrirent?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: S.M.Sanjay04
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
      
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  jackson:
    default-property-inclusion: NON_NULL

# JWT Configuration
jwt:
  secret: agrirent_super_secret_2026
  expiration: 2592000000  # 30 days in milliseconds

# Logging
logging:
  level:
    com.agrirent: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
